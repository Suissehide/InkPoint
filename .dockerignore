**/node_modules
**/dist
.git
.github
docs
.superpowers
.idea
# Tous les `.env` : `back/.env` entrait dans l'étage de build par le
# `COPY back/ back/` de `Dockerfile.back`. Les `.env.example` ne sont pas
# concernés, le motif porte sur le nom exact.
**/.env
*.log
