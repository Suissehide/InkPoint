# Format de replay et rejeu sans tête — plan d'implémentation

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Enregistrer une partie sous forme de `{ graine, entrées, cartes }` et la rejouer sans écran, pour qu'un serveur puisse recalculer un score au lieu de croire celui qu'on lui envoie.

**Architecture:** Trois variables de progression sortent de `front/src/app/game.ts` pour entrer dans `sim/`, où le tirage des cartes vit déjà. Un `sim/replay/format.ts` encode et décode un tampon binaire ; un `sim/replay/run.ts` le rejoue, réutilisable tel quel par le back à l'étape 3. La compression reste hors de `sim/`, chez l'appelant. Un hachage des sources de `sim/`, gravé au build, empêche de rejouer un replay sous une version qui n'est pas la sienne.

**Tech Stack:** TypeScript 5.7, Vitest 2.1, bitECS 0.3, Biome 2.4.5, `node:crypto` et `node:zlib` pour l'outillage.

Spec de référence : `docs/superpowers/specs/2026-08-03-replay-design.md`.
Vue d'ensemble : `docs/superpowers/specs/2026-08-02-leaderboard-architecture-design.md`.

## Global Constraints

- **Français** pour tout commentaire, nom de test et message de commit. **Les identifiants restent en anglais** — variables, fonctions, propriétés, constantes, sans exception.
- **Conventional Commits**, imposé par commitlint. Les commits de fusion aussi : `merge:` est refusé.
- **Biome 2.4.5** : `semicolons: asNeeded`, guillemets simples, `lineWidth: 100`, espaces. `npx biome check` doit être **totalement propre, zéro avertissement, aucun `biome-ignore`**. La règle `useConsistentArrayType` impose `T[]` et jamais `Array<T>`.
- **`noUncheckedIndexedAccess` est actif** : tout accès indexé demande `!` ou une garde.
- **`sim/purity.test.ts` s'applique à tout ce qu'on ajoute sous `sim/`** : aucun `Math.random`, `Date.now`, `new Date`, aucune API navigateur, aucun import de `render`/`ui`/`app`/`audio`, aucun transcendant de `Math`, aucun opérateur `**`. `DataView` et `BigInt` sont autorisés (`sim/math.ts` s'en sert déjà). `sim/scripts/` est exempté.
- **L'installation se fait depuis la racine** (`npm install`, `npm install -w front …`) : le lockfile des workspaces y vit, et c'est `<racine>/node_modules` qui rend `bitecs` résolvable depuis `sim/`.
- **Ne jamais `git add -A`.** Plusieurs sessions partagent ce dépôt ; stager les fichiers touchés, explicitement, et relire `git status --short` avant de valider.
- **Ne jamais tuer un processus par motif** (`pkill -f …`) : une session voisine a déjà perdu ses serveurs de dev comme ça. Suivre son propre pid, ou laisser tourner.
- **Vérifier l'état vert depuis le commit**, dans un worktree détaché, et non depuis l'arbre où l'on vient d'éditer. Un fichier à la fois indexé et modifié apparaît `MM` et échappe aux filtres naïfs de `git status`.
- Ne jamais pousser vers `origin`.

## Structure des fichiers

| Fichier | Responsabilité |
| --- | --- |
| `sim/upgrades/progress.ts` | **Créé.** `RunProgress` et ses trois opérations. L'historique d'une run dont dépend le tirage. |
| `sim/upgrades/progress.test.ts` | **Créé.** |
| `sim/version.generated.ts` | **Créé, généré, committé.** L'empreinte des sources de `sim/`. |
| `sim/scripts/sim-source-hash.ts` | **Créé.** Le calcul de l'empreinte, sans effet de bord — pour que le test puisse l'importer. |
| `sim/scripts/gen-version.ts` | **Créé.** Écrit le fichier généré. |
| `sim/version.test.ts` | **Créé.** Vérifie que l'empreinte committée correspond aux sources. |
| `sim/replay/format.ts` | **Créé.** Encodage et décodage du tampon binaire, sans compression. |
| `sim/replay/format.test.ts` | **Créé.** Aller-retour, bornes, refus d'un en-tête invalide. |
| `sim/replay/run.ts` | **Créé.** Le rejeu. Pur, réutilisable par le back. |
| `sim/replay/run.test.ts` | **Créé.** Rejeu bout en bout comparé à l'empreinte de la run directe. |
| `sim/scripts/replay.ts` | **Créé.** La CLI : gunzip, décode, rejoue, affiche. |
| `front/src/app/replay-recorder.ts` | **Créé.** L'enregistreur, et le téléchargement en développement. |
| `sim/input.ts` | **Modifié.** Accueille `QUANTUM` et `INPUT_FIELDS`, plus le garde-fou qui rend une addition de champ visible au compilateur. |
| `front/src/app/mouse.ts` | **Modifié.** Importe `QUANTUM` au lieu de le redéclarer. |
| `front/src/app/game.ts` | **Modifié.** `RunProgress` remplace trois variables locales ; l'enregistreur est branché. |
| `front/package.json` | **Modifié.** Scripts `version` et `replay`. |
| `.github/workflows/ci.yml` | **Modifié.** Contrôle que l'empreinte committée n'a pas dérivé. |

## Note de coordination

Au moment d'écrire ce plan, **une autre session édite `front/src/app/game.ts`** (une animation de volet sur le bandeau de succès). Les tâches 2 et 6 touchent ce fichier. Demander un worktree dédié avant de commencer plutôt que de travailler dans l'arbre partagé — c'est le motif que le chantier précédent a validé.

---

### Task 1 : `RunProgress`, l'historique d'une run

Le tirage `drawUpgrades(rng, state)` vit déjà dans `sim/upgrades/draw.ts` et attend un `DrawState`. Ce que cette tâche apporte, c'est le porteur de cet état, du côté de la simulation.

**Files:**
- Create: `sim/upgrades/progress.ts`, `sim/upgrades/progress.test.ts`

**Interfaces:**
- Consumes: `DrawState` et `UpgradeDef` (`sim/upgrades/draw.ts`), `RunStats` (`sim/upgrades/stats.ts`), `SimWorld` (`sim/world.ts`), `PowerUpKind` (`sim/data/powerups.ts`).
- Produces: `RunProgress`, `createRunProgress()`, `absorbEvents(progress, world)`, `takeUpgrade(card, stats, progress)`.

- [ ] **Step 1 : Écrire le test qui échoue**

Créer `sim/upgrades/progress.test.ts` :

```ts
import { describe, expect, it } from 'vitest'

import { UPGRADES } from '../data/upgrades'
import { createRunStats } from './stats'
import { absorbEvents, createRunProgress, takeUpgrade } from './progress'
import { ARENA, createWorld, type SimWorld } from '../world'

function newWorld(): SimWorld {
  return createWorld({ seed: 1, width: ARENA.width, height: ARENA.height })
}

describe('RunProgress', () => {
  it('démarre vide', () => {
    const progress = createRunProgress()
    expect(progress.ownedIds).toEqual([])
    expect(progress.mythicTaken).toBe(false)
    expect(progress.seenPowerups.size).toBe(0)
  })

  it('retient les power-ups ramassés depuis les événements du pas', () => {
    const world = newWorld()
    const progress = createRunProgress()
    world.events.push({ type: 'powerupPicked', kind: 1 })
    absorbEvents(progress, world)
    expect(progress.seenPowerups.size).toBe(1)
  })

  it('ignore un genre inconnu plutôt que d’enregistrer `undefined`', () => {
    const world = newWorld()
    const progress = createRunProgress()
    // `POWERUP_BY_ID` est un tableau creux : un identifiant hors plage rend
    // `undefined`, et l'insérer empoisonnerait le tirage des cartes.
    world.events.push({ type: 'powerupPicked', kind: 250 })
    absorbEvents(progress, world)
    expect(progress.seenPowerups.size).toBe(0)
  })

  it('n’enregistre pas deux fois le même genre', () => {
    const world = newWorld()
    const progress = createRunProgress()
    world.events.push({ type: 'powerupPicked', kind: 1 })
    world.events.push({ type: 'powerupPicked', kind: 1 })
    absorbEvents(progress, world)
    expect(progress.seenPowerups.size).toBe(1)
  })

  it('applique une carte : effets dans stats, historique dans progress', () => {
    const stats = createRunStats()
    const progress = createRunProgress()
    const card = UPGRADES.find((c) => c.rarity !== 'mythic')
    if (card === undefined) throw new Error('aucune carte non mythique')
    const before = stats.moveSpeed

    takeUpgrade(card, stats, progress)

    expect(progress.ownedIds).toEqual([card.id])
    expect(progress.mythicTaken).toBe(false)
    // `apply` a bien tourné : au moins une valeur de `stats` a bougé, ou une
    // règle est apparue. On ne teste pas *quelle* carte fait quoi — c'est le
    // travail de `draw.test.ts` et des tests de power-ups.
    expect(stats.moveSpeed !== before || stats.rules.size > 0).toBe(true)
  })

  it('lève `mythicTaken` sur une carte mythique, et retient les doublons', () => {
    const stats = createRunStats()
    const progress = createRunProgress()
    const mythic = UPGRADES.find((c) => c.rarity === 'mythic')
    const common = UPGRADES.find((c) => c.rarity !== 'mythic')
    if (mythic === undefined || common === undefined) throw new Error('catalogue incomplet')

    takeUpgrade(common, stats, progress)
    takeUpgrade(common, stats, progress)
    takeUpgrade(mythic, stats, progress)

    expect(progress.ownedIds).toEqual([common.id, common.id, mythic.id])
    expect(progress.mythicTaken).toBe(true)
  })
})
```

- [ ] **Step 2 : Lancer le test pour le voir échouer**

Run: `cd front && npx vitest run ../sim/upgrades/progress.test.ts`
Expected: FAIL, « Failed to resolve import "./progress" ».

- [ ] **Step 3 : Écrire `sim/upgrades/progress.ts`**

```ts
import { POWERUP_BY_ID, type PowerUpKind } from '../data/powerups'
import type { SimWorld } from '../world'
import type { UpgradeDef } from '../data/upgrades'
import type { RunStats } from './stats'

/**
 * L'historique d'une run dont dépend le tirage des cartes : exactement les trois
 * champs que `DrawState` réclame en plus de la vague.
 *
 * Vit dans `sim/` et non dans `front/src/app/` parce qu'il alimente un calcul
 * **déterministe** — même raison que le hitstop à l'étape 1. Sans cela, le rejeu
 * sans tête devrait importer le front pour reproduire l'offre de cartes, et
 * l'image Docker du back devrait embarquer l'arbre du front.
 *
 * Délibérément séparé de `RunStats` : celui-ci traverse `stepWorld` à chaque pas
 * et les systèmes le lisent, alors qu'aucun système ne lit `ownedIds`. Les
 * fusionner mettrait du poids mort dans le chemin chaud et brouillerait ce que
 * chacun des deux types signifie.
 */
export interface RunProgress {
  /** Ids des cartes prises, doublons compris — la pondération du tirage s'en sert. */
  ownedIds: string[]
  mythicTaken: boolean
  seenPowerups: Set<PowerUpKind>
}

export function createRunProgress(): RunProgress {
  return { ownedIds: [], mythicTaken: false, seenPowerups: new Set() }
}

/**
 * Absorbe les événements du pas qui vient de s'écouler. À appeler **après**
 * `stepWorld`, qui purge `world.events` en entrant : le tableau contient alors
 * les événements de ce pas-là.
 */
export function absorbEvents(progress: RunProgress, world: SimWorld): void {
  for (const event of world.events) {
    if (event.type === 'powerupPicked') {
      const kind = POWERUP_BY_ID[event.kind]
      // `POWERUP_BY_ID` est un tableau creux : un identifiant hors plage rend
      // `undefined`, et l'insérer dans le `Set` empoisonnerait le tirage.
      if (kind) {
        progress.seenPowerups.add(kind)
      }
    }
  }
}

/**
 * Applique une carte choisie. Les effets vont dans `stats`, que les systèmes
 * lisent ; l'historique va dans `progress`, que le prochain tirage lit.
 */
export function takeUpgrade(card: UpgradeDef, stats: RunStats, progress: RunProgress): void {
  card.apply(stats)
  progress.ownedIds.push(card.id)
  if (card.rarity === 'mythic') {
    progress.mythicTaken = true
  }
}
```

- [ ] **Step 4 : Lancer le test pour le voir passer**

Run: `cd front && npx vitest run ../sim/upgrades/progress.test.ts`
Expected: PASS, six tests.

- [ ] **Step 5 : Vérifier la suite et le lint**

Run: `cd front && npm run lint && npm run typecheck && npm test`
Expected: PASS. `REFERENCE_DIGEST` inchangée — cette tâche n'ajoute que du code non appelé.

- [ ] **Step 6 : Commit**

```bash
git add sim/upgrades/progress.ts sim/upgrades/progress.test.ts
git commit -m "feat(sim): l'historique d'une run, du côté de la simulation"
```

---

### Task 2 : Le jeu utilise `RunProgress`

Déplacement de comportement nul : trois variables locales deviennent un objet, et deux appels changent de nom.

**Files:**
- Modify: `front/src/app/game.ts`

**Interfaces:**
- Consumes: `RunProgress`, `createRunProgress`, `absorbEvents`, `takeUpgrade` (tâche 1).
- Produces: rien de nouveau. Le `progress` est en portée dans `startRun`, `onWaveEnded` et `onCardChosen`.

- [ ] **Step 1 : Remplacer les trois variables**

Dans `front/src/app/game.ts`, supprimer `let ownedIds: string[] = []` et `let mythicTaken = false` (autour des lignes 100-101, à retrouver par symbole — une autre session édite ce fichier et les numéros dérivent), et les remettre à zéro là où elles l'étaient :

```ts
  let progress = createRunProgress()
```

et dans la remise à zéro de début de run, `progress = createRunProgress()`.

- [ ] **Step 2 : Alimenter `seenPowerups` depuis les événements**

Dans la boucle `onStep`, après `stepWorld` et à côté des autres consommateurs d'événements (`applyJuice`, `applyAudio`, `handleSimEvents`) :

```ts
        absorbEvents(progress, run.world)
```

Le traqueur de succès continue de calculer son propre `powerupsPicked` : les deux dérivent la même chose des mêmes événements, et son code n'est pas touché.

- [ ] **Step 3 : Brancher le tirage sur `progress`**

Dans `onWaveEnded`, `drawUpgrades` reçoit désormais l'état de `progress` :

```ts
    const cards = drawUpgrades(rng, {
      wave,
      ownedIds: progress.ownedIds,
      mythicTaken: progress.mythicTaken,
      seenPowerups: progress.seenPowerups,
    })
```

- [ ] **Step 4 : Passer par `takeUpgrade`**

Dans `onCardChosen`, remplacer les trois lignes `card.apply(run.stats)`, `ownedIds.push(card.id)` et le `if (card.rarity === 'mythic')` par :

```ts
    takeUpgrade(card, run.stats, progress)
```

- [ ] **Step 5 : Vérifier**

Run: `cd front && npm run lint && npm run typecheck && npm test`
Expected: PASS, `REFERENCE_DIGEST` inchangée. `drawUpgrades` n'est pas appelé par `stepWorld`, et la run de référence n'utilise aucune carte — elle active les power-ups directement.

- [ ] **Step 6 : Jouer une partie**

Run: `cd front && npm run dev`
Prendre au moins deux cartes et vérifier que l'offre reste cohérente : pas de doublon impossible, la garantie de pitié mythique intacte. Si le navigateur n'est pas pilotable dans l'environnement, le dire dans le rapport plutôt que de l'affirmer.

- [ ] **Step 7 : Commit**

```bash
git add front/src/app/game.ts
git commit -m "refactor(front): le jeu passe par RunProgress pour l'historique de run"
```

---

### Task 3 : L'empreinte des sources de `sim/`

Un replay n'est valide que sous la version qui l'a produit. Cette tâche fabrique l'identifiant de version, et le contrôle qui l'empêche de dériver.

**Files:**
- Create: `sim/scripts/sim-source-hash.ts`, `sim/scripts/gen-version.ts`, `sim/version.generated.ts`, `sim/version.test.ts`
- Modify: `front/package.json`, `.github/workflows/ci.yml`

**Interfaces:**
- Consumes: rien.
- Produces: `SIM_VERSION` — une chaîne de 16 caractères hexadécimaux (8 octets) exportée par `sim/version.generated.ts`, et `simSourceHash()` exportée par le générateur pour que le test puisse recalculer.

- [ ] **Step 1 : Écrire le calcul du hachage, sans effet de bord**

Deux fichiers et non un, pour une raison qui compte : le test de la dérive doit importer le
calcul, et un module qui **écrit un fichier au chargement** réécrirait
`sim/version.generated.ts` chaque fois qu'un test l'importe — le contrôle de dérive ne
pourrait alors jamais rougir, puisqu'il régénérerait ce qu'il est censé comparer.

Créer `sim/scripts/sim-source-hash.ts`, qui ne fait que calculer. Il vit sous
`sim/scripts/`, exempté du test de pureté, et peut donc utiliser `node:fs` et `node:crypto`.

```ts
/**
 * Calcule l'empreinte des sources de `sim/` et l'écrit dans
 * `sim/version.generated.ts`. Depuis `front/` : `npm run version`.
 *
 * Cette empreinte identifie la version de simulation qu'un replay suppose. Elle
 * est **gravée au build** parce qu'un navigateur ne peut pas lire les fichiers de
 * `sim/` alors que Node peut : la calculer à l'exécution ferait porter aux deux
 * côtés des valeurs différentes dès qu'un build est périmé.
 *
 * Choix conservateur assumé : *toute* modification de `sim/` change l'empreinte,
 * y compris un commentaire. Une invalidation inutile n'a jamais fait calculer un
 * score faux ; l'inverse, si.
 */
import { createHash } from 'node:crypto'
import { readdirSync, readFileSync, writeFileSync } from 'node:fs'
import { join } from 'node:path'
import { fileURLToPath, URL } from 'node:url'

const SIM_DIR = fileURLToPath(new URL('..', import.meta.url))

/**
 * Les fichiers dont le contenu décide du comportement d'un rejeu. Sont exclus :
 * les tests (ils n'entrent pas dans une run), `scripts/` (outillage), et
 * `version.generated.ts` lui-même — l'inclure rendrait l'empreinte impossible à
 * calculer, puisqu'elle dépendrait de sa propre valeur.
 */
function sourceFiles(dir: string, prefix = ''): string[] {
  return readdirSync(dir, { withFileTypes: true })
    .flatMap((entry) => {
      const rel = prefix === '' ? entry.name : `${prefix}/${entry.name}`
      if (entry.isDirectory()) {
        return entry.name === 'scripts' ? [] : sourceFiles(join(dir, entry.name), rel)
      }
      if (!entry.name.endsWith('.ts')) return []
      if (entry.name.endsWith('.test.ts')) return []
      if (rel === 'version.generated.ts') return []
      return [rel]
    })
    .sort()
}

export function simSourceHash(): string {
  const hash = createHash('sha256')
  for (const rel of sourceFiles(SIM_DIR)) {
    // Le chemin entre dans le hachage : renommer un fichier sans toucher son
    // contenu change bel et bien la simulation qu'on rejoue.
    hash.update(rel)
    hash.update('\0')
    hash.update(readFileSync(join(SIM_DIR, rel)))
    hash.update('\0')
  }
  return hash.digest('hex').slice(0, 16)
}
```

- [ ] **Step 2 : Écrire le générateur, qui écrit**

Créer `sim/scripts/gen-version.ts` :

```ts
/** Écrit `sim/version.generated.ts`. Depuis `front/` : `npm run version:sim`. */
import { writeFileSync } from 'node:fs'
import { fileURLToPath, URL } from 'node:url'

import { simSourceHash } from './sim-source-hash'

const generated = `/**
 * Généré par sim/scripts/gen-version.ts — ne pas éditer à la main.
 * Régénérer avec \`npm run version\` depuis front/, après toute modification
 * volontaire de sim/. La CI vérifie que ce fichier n'a pas dérivé.
 */
export const SIM_VERSION = '${simSourceHash()}'
`

const out = fileURLToPath(new URL('../version.generated.ts', import.meta.url))
writeFileSync(out, generated)
console.log(`écrit : ${out}`)
```

- [ ] **Step 3 : Ajouter le script à `front/package.json`**

```json
    "version:sim": "tsx ../sim/scripts/gen-version.ts",
```

Nommé `version:sim` et non `version` : `npm version` est une commande npm intégrée, et un script homonyme s'exécuterait à des moments inattendus.

- [ ] **Step 3 : Générer**

Run: `cd front && npm run version:sim`
Expected: `écrit : …/sim/version.generated.ts`

- [ ] **Step 4 : Écrire le test de dérive**

Créer `sim/version.test.ts` :

```ts
import { describe, expect, it } from 'vitest'

import { simSourceHash } from './scripts/sim-source-hash'
import { SIM_VERSION } from './version.generated'

describe('empreinte des sources de sim/', () => {
  it('correspond aux sources actuelles', () => {
    // Rouge = quelqu'un a modifié `sim/` sans régénérer. Ce n'est pas le test
    // qui est trop strict : un replay enregistré sous l'ancienne empreinte
    // serait rejoué sous une simulation différente, et rendrait un score faux.
    // `npm run version:sim` depuis front/.
    expect(SIM_VERSION).toBe(simSourceHash())
  })

  it('fait 16 caractères hexadécimaux', () => {
    expect(SIM_VERSION).toMatch(/^[0-9a-f]{16}$/)
  })
})
```

Ce test importe `node:crypto` et `node:fs` par transitivité, donc il ne peut pas tourner dans un navigateur. Il porte le suffixe `.test.ts` et vit dans `sim/`, or `vitest.browser.config.ts` rejoue `sim/**/*.test.ts` : **l'ajouter à l'`exclude` de cette configuration**, à côté de `sim/purity.test.ts`, avec la même raison en commentaire.

- [ ] **Step 5 : Vérifier que le test mord**

Ajouter temporairement un commentaire dans n'importe quel fichier de `sim/` (par exemple `sim/world.ts`), lancer :

Run: `cd front && npx vitest run ../sim/version.test.ts`
Expected: FAIL, l'empreinte ne correspond plus.

Retirer le commentaire, relancer, confirmer le vert. Un contrôle de dérive qu'on n'a jamais vu rougir est une supposition.

- [ ] **Step 6 : Ajouter le contrôle en CI**

Dans `.github/workflows/ci.yml`, job `check`, après `npm ci` :

```yaml
      # L'empreinte de `sim/` est committée : si régénérer produit un diff,
      # quelqu'un a modifié la simulation sans la régénérer, et les replays
      # enregistrés sous l'ancienne valeur seraient rejoués sous une autre.
      - run: npm run version:sim
        working-directory: front
      - run: git diff --exit-code sim/version.generated.ts
```

- [ ] **Step 7 : Vérifier et commiter**

Run: `cd front && npm run lint && npm run typecheck && npm test`
Expected: PASS.

```bash
git add sim/scripts/gen-version.ts sim/version.generated.ts sim/version.test.ts \
        front/package.json front/vitest.browser.config.ts .github/workflows/ci.yml
git commit -m "feat(sim): une empreinte des sources, pour refuser un replay périmé"
```

---

### Task 4 : `sim/replay/format.ts`

Encodage et décodage, sans compression. Rien n'est encore branché.

**Files:**
- Create: `sim/replay/format.ts`, `sim/replay/format.test.ts`

**Interfaces:**
- Consumes: `SIM_VERSION` (tâche 3).
- Produces:
  ```ts
  export interface CardChoice { step: number; index: number }
  export interface Replay {
    simVersion: string
    seed: number
    inputs: Int16Array   // 2 entrées par pas : kx, ky
    choices: CardChoice[]
  }
  export function encodeReplay(replay: Replay): Uint8Array
  export function decodeReplay(bytes: Uint8Array): Replay
  export const REPLAY_FORMAT_VERSION = 1
  ```
  Et, dans `sim/input.ts`, le domicile unique du pas de quantification :
  ```ts
  /** Pas de quantification des entrées — prérequis du netcode v3. */
  export const QUANTUM = 1 / 128
  ```
  `decodeReplay` lève une `Error` au message explicite sur une magie, une version de format ou une longueur invalides. Elle **ne vérifie pas** `simVersion` : c'est le rejeu qui décide quoi faire d'un replay d'une autre version.

- [ ] **Step 1 : Donner à `QUANTUM` un domicile unique**

`front/src/app/mouse.ts` déclare `const QUANTUM = 1 / 128`. Le rejeu a besoin de la même
valeur, et deux constantes qui doivent rester égales sous peine de casser les replays **en
silence** ne doivent pas exister en deux endroits.

Déplacer la déclaration dans `sim/input.ts`, où vit déjà `InputState` — c'est une propriété du
contrat d'entrée, pas du format de replay. Et, dans le même fichier, énumérer les champs, avec
un garde-fou qui rend impossible d'en ajouter un sans l'enregistrer :

```ts
/**
 * Pas de quantification des entrées — prérequis du netcode v3, et ce qui rend
 * l'enregistrement d'un replay sans perte : `1/128` valant `2⁻⁷`, `k · 2⁻⁷` est
 * exactement représentable en f64.
 */
export const QUANTUM = 1 / 128

/**
 * Les champs d'`InputState`, dans l'ordre où un replay les enregistre.
 *
 * Cette liste existe pour que le format de replay suive `InputState`
 * automatiquement. Un chantier voisin (manche virtuel et inclinaison en paysage)
 * ajoute un champ `speedCap` : sans cette liste, il faudrait se souvenir d'aller
 * étendre le format, et l'oublier ferait rejouer une partie mobile avec un
 * plafond de vitesse manquant — silencieusement.
 */
export const INPUT_FIELDS = ['moveX', 'moveY'] as const satisfies readonly (keyof InputState)[]

/**
 * Garde-fou : un champ ajouté à `InputState` et absent d'`INPUT_FIELDS` casse la
 * compilation, en **nommant** le champ manquant. Vérifié : ajouter `speedCap` à
 * `InputState` seul produit
 * `error TS2322: Type 'true' is not assignable to type '"speedCap"'`.
 */
type MissingInputField = Exclude<keyof InputState, (typeof INPUT_FIELDS)[number]>
const _noMissingInputField: MissingInputField extends never ? true : MissingInputField = true
void _noMissingInputField
```

et faire importer `mouse.ts` depuis `@sim/input` au lieu de redéclarer `QUANTUM`. Vérifier que
la suite reste verte : c'est la même valeur, donc rien ne doit bouger.

**Cela répond à la « coordination requise » de
`docs/superpowers/specs/2026-08-03-mobile-paysage-joystick-design.md`**, qui demande que
`speedCap` soit ajouté au format de replay plutôt que contourné. Avec cette liste, leur
chantier n'a qu'une ligne à ajouter, et le compilateur le leur rappellera.

- [ ] **Step 2 : Écrire le test qui échoue**

Créer `sim/replay/format.test.ts` :

```ts
import { describe, expect, it } from 'vitest'

import { INPUT_FIELDS } from '../input'
import { SIM_VERSION } from '../version.generated'
import { decodeReplay, encodeReplay, type Replay } from './format'

function sample(steps: number, choices: { step: number; index: number }[] = []): Replay {
  const inputs = new Int16Array(steps * INPUT_FIELDS.length)
  // Balaie toute la plage de `k`, bornes comprises, quel que soit le nombre de champs.
  for (let i = 0; i < inputs.length; i++) {
    inputs[i] = ((i % 257) - 128) as number
  }
  return { simVersion: SIM_VERSION, seed: 0x1234abcd, inputs, choices }
}

describe('format de replay', () => {
  it('fait un aller-retour identique', () => {
    const before = sample(500, [
      { step: 2400, index: 0 },
      { step: 4800, index: 2 },
    ])
    const after = decodeReplay(encodeReplay(before))
    expect(after.simVersion).toBe(before.simVersion)
    expect(after.seed).toBe(before.seed)
    expect(after.choices).toEqual(before.choices)
    expect(Array.from(after.inputs)).toEqual(Array.from(before.inputs))
  })

  it('tient les bornes de k, -128 et 128', () => {
    const r = sample(0)
    const inputs = new Int16Array([-128, 128, 128, -128])
    const after = decodeReplay(encodeReplay({ ...r, inputs }))
    expect(Array.from(after.inputs)).toEqual([-128, 128, 128, -128])
  })

  it('accepte une run vide et sans choix', () => {
    const after = decodeReplay(encodeReplay(sample(0)))
    expect(after.inputs.length).toBe(0)
    expect(after.choices).toEqual([])
  })

  it('refuse une magie invalide', () => {
    const bytes = encodeReplay(sample(10))
    bytes[0] = 0x58
    expect(() => decodeReplay(bytes)).toThrow(/magie/i)
  })

  it('refuse une version de format inconnue', () => {
    const bytes = encodeReplay(sample(10))
    bytes[4] = 99
    expect(() => decodeReplay(bytes)).toThrow(/version de format/i)
  })

  it('refuse un tampon tronqué', () => {
    const bytes = encodeReplay(sample(10))
    expect(() => decodeReplay(bytes.subarray(0, bytes.length - 3))).toThrow(/tronqu/i)
  })
})
```

- [ ] **Step 3 : Lancer le test pour le voir échouer**

Run: `cd front && npx vitest run ../sim/replay/format.test.ts`
Expected: FAIL, import non résolu.

- [ ] **Step 4 : Écrire `sim/replay/format.ts`**

```ts
/**
 * Encodage binaire d'une partie : `{ graine, entrées, cartes }`, et rien d'autre.
 *
 * Ce qui n'y figure pas, et pourquoi : l'arène est une constante de `sim/world.ts`,
 * `timeScale` est produit par `hitstopSystem` **dans** la simulation, la graine du
 * tirage des cartes est dérivée (`seed + wave`), et les power-ups ramassés viennent
 * de `world.rng`. Une seule ligne du front écrit dans la simulation, `world.input`.
 *
 * Les entrées se stockent en entiers sans perte : `mouse.ts` quantifie déjà
 * `world.input` sur un pas de `1/128`, et `1/128` valant `2⁻⁷`, `k · 2⁻⁷` est
 * exactement représentable en f64. Mesuré sur 500 000 valeurs : l'aller-retour
 * `round(v · 128) / 128` est bit-identique, sans exception.
 *
 * **La compression n'est pas ici.** Elle appartient à l'appelant —
 * `CompressionStream` dans le navigateur, `node:zlib` dans Node — parce qu'une API
 * de compression est spécifique à la plateforme et que ce module doit rester
 * portable pour passer `purity.test.ts`.
 */

import { INPUT_FIELDS } from '../input'

export const REPLAY_FORMAT_VERSION = 1

const MAGIC = 0x494e4b52 // 'INKR'
const HEADER_BYTES = 4 + 1 + 8 + 4 + 4 + 2
const CHOICE_BYTES = 4 + 1

export interface CardChoice {
  /** Pas auquel la vague s'est terminée. Contrôle de cohérence au rejeu. */
  step: number
  /** Indice choisi parmi les cartes proposées, 0 à 2. */
  index: number
}

export interface Replay {
  simVersion: string
  seed: number
  /** `INPUT_FIELDS.length` entiers par pas, dans l'ordre d'`INPUT_FIELDS`. */
  inputs: Int16Array
  choices: CardChoice[]
}

export function encodeReplay(replay: Replay): Uint8Array {
  const steps = replay.inputs.length / INPUT_FIELDS.length
  const bytes = new Uint8Array(
    HEADER_BYTES + replay.choices.length * CHOICE_BYTES + replay.inputs.length * 2,
  )
  const view = new DataView(bytes.buffer)
  let at = 0

  view.setUint32(at, MAGIC)
  at += 4
  view.setUint8(at, REPLAY_FORMAT_VERSION)
  at += 1
  for (let i = 0; i < 8; i++) {
    view.setUint8(at + i, Number.parseInt(replay.simVersion.slice(i * 2, i * 2 + 2), 16))
  }
  at += 8
  view.setUint32(at, replay.seed)
  at += 4
  view.setUint32(at, steps)
  at += 4
  view.setUint16(at, replay.choices.length)
  at += 2

  for (const choice of replay.choices) {
    view.setUint32(at, choice.step)
    at += 4
    view.setUint8(at, choice.index)
    at += 1
  }
  for (const k of replay.inputs) {
    view.setInt16(at, k)
    at += 2
  }
  return bytes
}

export function decodeReplay(bytes: Uint8Array): Replay {
  if (bytes.length < HEADER_BYTES) {
    throw new Error('replay tronqué : en-tête incomplet')
  }
  const view = new DataView(bytes.buffer, bytes.byteOffset, bytes.byteLength)
  let at = 0

  if (view.getUint32(at) !== MAGIC) {
    throw new Error('magie absente : ce fichier n’est pas un replay InkPoint')
  }
  at += 4
  const formatVersion = view.getUint8(at)
  at += 1
  if (formatVersion !== REPLAY_FORMAT_VERSION) {
    throw new Error(
      `version de format ${formatVersion} inconnue, attendu ${REPLAY_FORMAT_VERSION}`,
    )
  }
  let simVersion = ''
  for (let i = 0; i < 8; i++) {
    simVersion += view.getUint8(at + i).toString(16).padStart(2, '0')
  }
  at += 8
  const seed = view.getUint32(at)
  at += 4
  const steps = view.getUint32(at)
  at += 4
  const choiceCount = view.getUint16(at)
  at += 2

  const expected =
    HEADER_BYTES + choiceCount * CHOICE_BYTES + steps * INPUT_FIELDS.length * 2
  if (bytes.length !== expected) {
    throw new Error(`replay tronqué ou trop long : ${bytes.length} octets, attendu ${expected}`)
  }

  const choices: CardChoice[] = []
  for (let i = 0; i < choiceCount; i++) {
    choices.push({ step: view.getUint32(at), index: view.getUint8(at + 4) })
    at += CHOICE_BYTES
  }
  const inputs = new Int16Array(steps * INPUT_FIELDS.length)
  for (let i = 0; i < inputs.length; i++) {
    inputs[i] = view.getInt16(at)
    at += 2
  }
  return { simVersion, seed, inputs, choices }
}
```

- [ ] **Step 5 : Lancer le test pour le voir passer**

Run: `cd front && npx vitest run ../sim/replay/format.test.ts`
Expected: PASS, six tests.

- [ ] **Step 6 : Vérifier la suite entière**

Run: `cd front && npm run lint && npm run typecheck && npm test && npm run test:browser:chromium`
Expected: PASS. Le rejeu inter-moteurs couvre désormais ce fichier — c'est voulu, l'encodage doit être identique partout.

- [ ] **Step 7 : Commit**

```bash
git add sim/replay/format.ts sim/replay/format.test.ts
git commit -m "feat(sim): le format binaire d'un replay, sans compression"
```

---

### Task 5 : `sim/replay/run.ts`

Le rejeu. Pur, et réutilisable tel quel par le back à l'étape 3.

**Files:**
- Create: `sim/replay/run.ts`, `sim/replay/run.test.ts`

**Interfaces:**
- Consumes: `Replay`, `QUANTUM` (tâche 4), `SIM_VERSION` (tâche 3), `RunProgress` et ses opérations (tâche 1), `createWorld`, `stepWorld`, `spawnPlayer`, `createRunStats`, `drawUpgrades`, `createRng`.
- Produces:
  ```ts
  export interface ReplayResult { score: number; wave: number; steps: number; alive: boolean }
  export function replayRun(replay: Replay): ReplayResult
  ```
  Lève une `Error` si `replay.simVersion !== SIM_VERSION`, ou si un choix de carte ne tombe pas sur une fin de vague, ou si son indice est hors des cartes proposées.

- [ ] **Step 1 : Écrire le test qui échoue**

Créer `sim/replay/run.test.ts`. Le test **enregistre lui-même** une run scriptée, ce qui rend la tâche indépendante de l'enregistreur du front (tâche 6).

```ts
import * as bitecs from 'bitecs'
import { describe, expect, it } from 'vitest'

import { grantInvulnerability } from '../invulnerability'
import { createRng } from '../rng'
import { spawnPlayer } from '../spawn'
import { stepWorld } from '../step'
import { absorbEvents, createRunProgress } from '../upgrades/progress'
import { createRunStats } from '../upgrades/stats'
import { SIM_VERSION } from '../version.generated'
import { ARENA, createWorld } from '../world'
import { INPUT_FIELDS, QUANTUM } from '../input'
import type { Replay } from './format'
import { replayRun } from './run'

const { resetGlobals } = bitecs as unknown as { resetGlobals: () => void }

/** Quantifie comme `app/mouse.ts`, pour que la run enregistrée soit rejouable. */
const quantize = (v: number): number => Math.round(v / QUANTUM) * QUANTUM

/**
 * Joue une run scriptée en enregistrant ses entrées, et rend le replay obtenu
 * plus le résultat direct. Aucune carte : le tirage demande une interaction, et
 * il est couvert par ses propres tests.
 */
function recordScriptedRun(seed: number, steps: number) {
  resetGlobals()
  const world = createWorld({ seed, width: ARENA.width, height: ARENA.height })
  spawnPlayer(world)
  const stats = createRunStats()
  const progress = createRunProgress()
  const inputRng = createRng(seed * 7919 + 13)
  const inputs = new Int16Array(steps * 2)

  for (let i = 0; i < steps; i++) {
    if (i % 20 === 0) {
      world.input.moveX = quantize(inputRng.range(-1, 1))
      world.input.moveY = quantize(inputRng.range(-1, 1))
    }
    if (i % 60 === 0) {
      grantInvulnerability(world, world.playerEid, 1200)
    }
    for (let f = 0; f < INPUT_FIELDS.length; f++) {
      inputs[i * INPUT_FIELDS.length + f] = Math.round(world.input[INPUT_FIELDS[f]!] / QUANTUM)
    }
    stepWorld(world, stats)
    absorbEvents(progress, world)
  }

  const replay: Replay = { simVersion: SIM_VERSION, seed, inputs, choices: [] }
  return { replay, direct: { score: world.score, wave: world.wave, alive: world.alive } }
}

describe('rejeu', () => {
  it('reproduit exactement le résultat de la run directe', () => {
    const { replay, direct } = recordScriptedRun(1234, 1800)
    const result = replayRun(replay)
    // Au bit près, et non à une tolérance près : c'est l'objet de tout le
    // chantier précédent.
    expect(result.score).toBe(direct.score)
    expect(result.wave).toBe(direct.wave)
    expect(result.alive).toBe(direct.alive)
    expect(result.steps).toBe(1800)
  })

  it('refuse un replay d’une autre version de simulation', () => {
    const { replay } = recordScriptedRun(7, 60)
    expect(() => replayRun({ ...replay, simVersion: '0000000000000000' })).toThrow(/version/i)
  })

  it('absorbe un power-up ramassé au pas même où la vague se termine', () => {
    // Le pas de bascule, et le trou le plus dangereux de ce chantier. `game.ts`
    // absorbe la progression **avant** de traiter `waveEnded` (`absorbEvents` puis
    // `handleSimEvents`). `replayRun` doit faire pareil : inversé, un power-up
    // ramassé sur ce pas serait visible dans le tirage du jeu et invisible dans
    // celui du rejeu. La divergence ne toucherait que ce pas-là — donc rare,
    // tardive, et sur une partie qui monte au classement.
    //
    // On cherche une graine qui produit la collision plutôt que de la fabriquer :
    // `replayRun` est fermé, on ne peut pas y injecter d'événement.
    let found = -1
    for (let seed = 1; seed <= 400 && found < 0; seed++) {
      resetGlobals()
      const world = createWorld({ seed, width: ARENA.width, height: ARENA.height })
      spawnPlayer(world)
      const stats = createRunStats()
      for (let i = 0; i < 3600; i++) {
        grantInvulnerability(world, world.playerEid, 1200)
        stepWorld(world, stats)
        const kinds = new Set(world.events.map((e) => e.type))
        if (kinds.has('powerupPicked') && kinds.has('waveEnded')) {
          found = seed
          break
        }
      }
    }

    // Échoue franchement plutôt que de passer sans rien éprouver : un test de
    // scénario qui n'atteint pas son scénario est exactement le faux garde-fou
    // que ce chantier a déjà trouvé trois fois. Élargir la recherche, ou
    // instrumenter la collision autrement — mais ne pas assouplir.
    expect(found, 'aucune graine ≤ 400 ne produit la collision : élargir la recherche').toBeGreaterThan(0)
  })

  it('rejoue deux fois de suite le même replay à l’identique', () => {
    // `resetGlobals` doit être fait par `replayRun` : bitECS alloue les `eid`
    // depuis un compteur global au module, et sans remise à zéro le second
    // rejeu hérite du compteur du premier.
    const { replay } = recordScriptedRun(99, 600)
    expect(replayRun(replay)).toEqual(replayRun(replay))
  })
})
```

- [ ] **Step 2 : Lancer le test pour le voir échouer**

Run: `cd front && npx vitest run ../sim/replay/run.test.ts`
Expected: FAIL, import non résolu.

- [ ] **Step 3 : Écrire `sim/replay/run.ts`**

```ts
import * as bitecs from 'bitecs'

import { createRng } from '../rng'
import { spawnPlayer } from '../spawn'
import { stepWorld } from '../step'
import { drawUpgrades } from '../upgrades/draw'
import { absorbEvents, createRunProgress, takeUpgrade } from '../upgrades/progress'
import { createRunStats } from '../upgrades/stats'
import { SIM_VERSION } from '../version.generated'
import { ARENA, createWorld } from '../world'
import { INPUT_FIELDS, QUANTUM } from '../input'
import type { Replay } from './format'

/**
 * bitECS alloue les `eid` depuis un compteur **global au module**, ce que ses
 * propres types ne déclarent pas mais que son build JS exporte. Sans remise à
 * zéro, un second rejeu dans le même processus hérite du compteur du premier et
 * ses `eid` sont décalés — un serveur qui vérifie deux replays de suite
 * calculerait le second de travers.
 */
const { resetGlobals } = bitecs as unknown as { resetGlobals: () => void }

export interface ReplayResult {
  score: number
  wave: number
  steps: number
  alive: boolean
}

export function replayRun(replay: Replay): ReplayResult {
  if (replay.simVersion !== SIM_VERSION) {
    throw new Error(
      `replay enregistré sous la version ${replay.simVersion}, ` +
        `simulation actuelle ${SIM_VERSION} — rejeu impossible`,
    )
  }

  resetGlobals()
  const world = createWorld({ seed: replay.seed, width: ARENA.width, height: ARENA.height })
  spawnPlayer(world)
  const stats = createRunStats()
  const progress = createRunProgress()

  const steps = replay.inputs.length / INPUT_FIELDS.length
  let nextChoice = 0

  for (let i = 0; i < steps; i++) {
    // Tous les champs d'`InputState`, dans l'ordre d'`INPUT_FIELDS` : le jour où
    // le chantier du manche virtuel ajoute `speedCap`, cette boucle le rejoue
    // sans qu'on ait à y revenir.
    for (let f = 0; f < INPUT_FIELDS.length; f++) {
      world.input[INPUT_FIELDS[f]!] = replay.inputs[i * INPUT_FIELDS.length + f]! * QUANTUM
    }
    stepWorld(world, stats)
    absorbEvents(progress, world)

    for (const event of world.events) {
      if (event.type !== 'waveEnded') continue
      const choice = replay.choices[nextChoice]
      if (choice === undefined) {
        throw new Error(`vague ${event.wave} terminée au pas ${i} sans choix enregistré`)
      }
      if (choice.step !== i) {
        throw new Error(
          `choix ${nextChoice} annoncé au pas ${choice.step}, vague terminée au pas ${i}`,
        )
      }
      const cards = drawUpgrades(createRng(replay.seed + event.wave), {
        wave: event.wave,
        ownedIds: progress.ownedIds,
        mythicTaken: progress.mythicTaken,
        seenPowerups: progress.seenPowerups,
      })
      const card = cards[choice.index]
      if (card === undefined) {
        throw new Error(
          `indice ${choice.index} hors des ${cards.length} cartes proposées à la vague ${event.wave}`,
        )
      }
      takeUpgrade(card, stats, progress)
      nextChoice++
    }
  }

  if (nextChoice !== replay.choices.length) {
    throw new Error(
      `${replay.choices.length} choix enregistrés, ${nextChoice} fins de vague rencontrées`,
    )
  }

  return { score: world.score, wave: world.wave, steps, alive: world.alive }
}
```

- [ ] **Step 4 : Lancer le test pour le voir passer**

Run: `cd front && npx vitest run ../sim/replay/run.test.ts`
Expected: PASS, trois tests.

Si le premier test échoue sur `score`, **ne pas ajuster de tolérance** : c'est que l'enregistrement ou le rejeu diverge, et le chantier précédent a établi que ça se vérifie au bit près ou pas du tout. Comparer pas à pas en instrumentant les deux boucles, et rapporter.

- [ ] **Step 5 : Vérifier la suite entière**

Run: `cd front && npm run lint && npm run typecheck && npm test && npm run test:browser:chromium`
Expected: PASS.

- [ ] **Step 6 : Commit**

```bash
git add sim/replay/run.ts sim/replay/run.test.ts
git commit -m "feat(sim): rejouer un replay sans écran, et refuser une version étrangère"
```

---

### Task 6 : L'enregistreur, et le fichier qui sort du jeu

**Files:**
- Create: `front/src/app/replay-recorder.ts`, `front/src/app/replay-recorder.test.ts`
- Modify: `front/src/app/game.ts`

**Interfaces:**
- Consumes: `encodeReplay`, `QUANTUM`, `type Replay` (tâche 4), `SIM_VERSION` (tâche 3).
- Produces:
  ```ts
  export interface ReplayRecorder {
    /** À appeler juste après `writeInto`, avant `stepWorld`. */
    step(input: InputState): void
    /** À appeler quand le joueur choisit une carte. */
    choose(index: number): void
    reset(seed: number): void
    build(): Replay
  }
  export function createReplayRecorder(seed: number): ReplayRecorder
  ```

- [ ] **Step 1 : Écrire le test qui échoue**

Créer `front/src/app/replay-recorder.test.ts` :

```ts
import { describe, expect, it } from 'vitest'

import { QUANTUM } from '@sim/input'
import { SIM_VERSION } from '@sim/version.generated'
import { createReplayRecorder } from './replay-recorder'

describe('enregistreur de replay', () => {
  it('convertit les entrées en entiers, sans perte', () => {
    const rec = createReplayRecorder(42)
    rec.step({ moveX: 1, moveY: -1 })
    rec.step({ moveX: 0, moveY: 0 })
    rec.step({ moveX: 64 * QUANTUM, moveY: -64 * QUANTUM })
    const replay = rec.build()
    // Deux champs aujourd'hui (`moveX`, `moveY`) ; l'attente se construit depuis
    // `INPUT_FIELDS` pour ne pas casser le jour où un champ s'ajoute.
    expect(replay.inputs.length).toBe(3 * INPUT_FIELDS.length)
    expect(replay.inputs[0]).toBe(128)
    expect(replay.inputs[1]).toBe(-128)
    expect(replay.inputs[INPUT_FIELDS.length]).toBe(0)
    expect(replay.seed).toBe(42)
    expect(replay.simVersion).toBe(SIM_VERSION)
  })

  it('associe un choix au pas courant', () => {
    const rec = createReplayRecorder(1)
    rec.step({ moveX: 0, moveY: 0 })
    rec.step({ moveX: 0, moveY: 0 })
    rec.choose(2)
    expect(rec.build().choices).toEqual([{ step: 1, index: 2 }])
  })

  it('repart de zéro à la remise à zéro', () => {
    const rec = createReplayRecorder(1)
    rec.step({ moveX: 1, moveY: 1 })
    rec.choose(0)
    rec.reset(7)
    const replay = rec.build()
    expect(replay.seed).toBe(7)
    expect(replay.inputs.length).toBe(0)
    expect(replay.choices).toEqual([])
  })
})
```

Le choix se rattache au **dernier pas joué** (`step - 1`) : une vague se termine pendant un pas, et le joueur choisit ensuite. C'est ce pas-là que `run.ts` compare à `choice.step`.

- [ ] **Step 2 : Lancer le test pour le voir échouer**

Run: `cd front && npx vitest run src/app/replay-recorder.test.ts`
Expected: FAIL, import non résolu.

- [ ] **Step 3 : Écrire `front/src/app/replay-recorder.ts`**

```ts
import { INPUT_FIELDS, QUANTUM, type InputState } from '@sim/input'
import { encodeReplay, type CardChoice, type Replay } from '@sim/replay/format'
import { SIM_VERSION } from '@sim/version.generated'

/**
 * Accumule de quoi rejouer la partie en cours : la graine, un couple d'entiers
 * par pas, et les cartes choisies.
 *
 * Les entrées sont déjà sur la grille `1/128` quand elles arrivent ici
 * (`app/mouse.ts`), donc `round(v / QUANTUM)` est une conversion exacte et non
 * une quantification : le jeu ne change pas d'un iota parce qu'on enregistre.
 *
 * Tourne **toujours**, y compris en production : 144 Ko en mémoire pour dix
 * minutes est sans conséquence, et l'étape 3 aura besoin du replay de n'importe
 * quelle partie. Seul le *téléchargement* est réservé au développement.
 */
export interface ReplayRecorder {
  /** À appeler juste après `writeInto` et avant `stepWorld`. */
  step(input: InputState): void
  choose(index: number): void
  reset(seed: number): void
  build(): Replay
}

export function createReplayRecorder(seed: number): ReplayRecorder {
  let currentSeed = seed
  let inputs: number[] = []
  let choices: CardChoice[] = []

  return {
    step(input: InputState): void {
      // Itère `INPUT_FIELDS` plutôt que de nommer les champs : un champ ajouté à
      // `InputState` est enregistré sans qu'on touche ici.
      for (const field of INPUT_FIELDS) {
        inputs.push(Math.round(input[field] / QUANTUM))
      }
    },
    choose(index: number): void {
      // Rattaché au dernier pas joué : la vague s'est terminée pendant lui, le
      // choix vient après. C'est ce pas que `replayRun` recoupe.
      choices.push({ step: inputs.length / INPUT_FIELDS.length - 1, index })
    },
    reset(nextSeed: number): void {
      currentSeed = nextSeed
      inputs = []
      choices = []
    },
    build(): Replay {
      return {
        simVersion: SIM_VERSION,
        seed: currentSeed,
        inputs: Int16Array.from(inputs),
        choices: [...choices],
      }
    },
  }
}

/**
 * Écrit le replay dans les téléchargements. Réservé au développement : en
 * production le fichier partira au serveur (étape 3), pas sur le disque.
 */
export async function downloadReplay(replay: Replay): Promise<void> {
  const bytes = encodeReplay(replay)
  const gzipped = await new Response(
    new Blob([bytes]).stream().pipeThrough(new CompressionStream('gzip')),
  ).arrayBuffer()
  const url = URL.createObjectURL(new Blob([gzipped], { type: 'application/gzip' }))
  const link = document.createElement('a')
  link.href = url
  link.download = `partie-${replay.seed}.bin`
  link.click()
  URL.revokeObjectURL(url)
}
```

- [ ] **Step 4 : Lancer le test pour le voir passer**

Run: `cd front && npx vitest run src/app/replay-recorder.test.ts`
Expected: PASS, trois tests.

- [ ] **Step 5 : Brancher dans `game.ts`**

Quatre points d'accroche, à retrouver par symbole — une autre session édite ce fichier :

1. à la création de la run, `const recorder = createReplayRecorder(run.seed)` ; et `recorder.reset(run.seed)` là où la run repart ;
2. dans `onStep`, **juste après** `source.writeInto(run.world.input, playerMotion())` et **avant** `stepWorld` : `recorder.step(run.world.input)` — l'ordre importe, c'est l'entrée consommée par ce pas qu'on enregistre ;
3. le rappel de `upgradeScreen.show` ne reçoit que la carte, pas son indice. Le tableau `cards` étant en portée dans `onWaveEnded`, l'envelopper là :

```ts
    upgradeScreen.show(cards, wave, (card) => {
      recorder.choose(cards.indexOf(card))
      onCardChosen(card)
    })
```
4. à la mort, en développement seulement :

```ts
        if (import.meta.env.DEV) {
          void downloadReplay(recorder.build())
        }
```

- [ ] **Step 6 : Vérifier**

Run: `cd front && npm run lint && npm run typecheck && npm test && npm run build`
Expected: PASS. `REFERENCE_DIGEST` inchangée : l'enregistreur lit, il n'écrit pas dans le monde.

- [ ] **Step 7 : Jouer une partie et récupérer le fichier**

Run: `cd front && npm run dev`
Jouer, prendre au moins une carte, mourir. Un `partie-<graine>.bin` doit apparaître dans les téléchargements. Noter le score affiché à l'écran de fin — c'est lui que la tâche 7 comparera. Si le navigateur n'est pas pilotable dans l'environnement, le dire plutôt que de l'affirmer.

- [ ] **Step 8 : Commit**

```bash
git add front/src/app/replay-recorder.ts front/src/app/replay-recorder.test.ts \
        front/src/app/game.ts
git commit -m "feat(front): enregistrer chaque partie, et la verser en développement"
```

---

### Task 7 : La CLI, et la boucle de vérification à la main

**Files:**
- Create: `sim/scripts/replay.ts`
- Modify: `front/package.json`, `README.md`

**Interfaces:**
- Consumes: `decodeReplay` (tâche 4), `replayRun` (tâche 5).
- Produces: `npm run replay <fichier>`.

- [ ] **Step 1 : Écrire la CLI**

Créer `sim/scripts/replay.ts`. Sous `sim/scripts/`, donc exempté du test de pureté et libre d'utiliser `node:fs` et `node:zlib`.

```ts
/**
 * Rejoue un fichier de replay et affiche le score recalculé.
 * Depuis `front/` : `npm run replay ../partie-123.bin`
 *
 * C'est le pendant en ligne de commande de ce que le worker de vérification
 * fera à l'étape 3 : `replayRun` est le même code, appelé au même endroit du
 * raisonnement — le score envoyé par un client n'est jamais cru.
 */
import { readFileSync } from 'node:fs'
import { gunzipSync } from 'node:zlib'

import { decodeReplay } from '../replay/format'
import { replayRun } from '../replay/run'

const path = process.argv[2]
if (path === undefined) {
  console.error('usage : npm run replay <fichier.bin>')
  process.exit(1)
}

const raw = readFileSync(path)
// Le navigateur gzippe avant d'écrire ; un fichier non compressé reste accepté,
// pour qu'un replay fabriqué à la main soit rejouable sans cérémonie.
const bytes = raw[0] === 0x1f && raw[1] === 0x8b ? gunzipSync(raw) : raw

const replay = decodeReplay(new Uint8Array(bytes))
const result = replayRun(replay)

console.log(`graine        ${replay.seed}`)
console.log(`version sim   ${replay.simVersion}`)
console.log(`pas           ${result.steps}`)
console.log(`cartes        ${replay.choices.length}`)
console.log(`vague         ${result.wave}`)
console.log(`vivant        ${result.alive ? 'oui' : 'non'}`)
console.log(`score         ${result.score}`)
```

- [ ] **Step 2 : Ajouter le script**

Dans `front/package.json` :

```json
    "replay": "tsx ../sim/scripts/replay.ts",
```

- [ ] **Step 3 : Rejouer le fichier de la tâche 6**

Run: `cd front && npm run replay ../partie-<graine>.bin`
Expected: le score affiché **égale exactement** celui noté à l'écran de fin.

S'il diffère, c'est le résultat le plus important de cette tâche et il ne doit pas être contourné : rapporter les deux valeurs, la graine, le nombre de pas. Ne rien ajuster.

- [ ] **Step 4 : Mesurer la taille**

Comparer la taille du `.bin` gzippé à `HEADER_BYTES + pas × INPUT_FIELDS.length × 2` octets bruts, et rapporter le ratio. La spec laisse ouvert l'encodage delta et l'empaquetage sur 9 bits : ils ne se justifient que si la mesure les réclame. **Mesurer, ne pas supposer.**

- [ ] **Step 5 : Documenter dans le README**

Dans « Development », ajouter :

```
npm run replay <file>   # replays a recorded run and recomputes its score
```

Et une phrase dans « Architecture » : une partie s'enregistre en `{ seed, inputs, cards }`, se rejoue sans écran, et le serveur du classement recalculera le score au lieu de croire celui du client. `sim/replay/run.ts` est le code que ce serveur appellera.

- [ ] **Step 6 : Vérification finale**

```bash
cd front && npm run lint && npm run typecheck && npm test && npm run test:browser && npm run build
cd .. && docker build -f deploy/Dockerfile --target app -t inkpoint:replay .
```
Expected: tout passe, les trois moteurs compris.

- [ ] **Step 7 : Commit**

```bash
git add sim/scripts/replay.ts front/package.json README.md
git commit -m "feat(sim): une CLI pour rejouer un replay et recalculer son score"
```

---

## Notes pour l'exécutant

**Le score rejoué doit égaler le score affiché, exactement.** Pas à une tolérance près. L'étape 1 a établi que la simulation rejoue au bit près sur trois moteurs ; si un écart apparaît ici, il vient de l'enregistrement ou du rejeu, pas de l'arithmétique. Le diagnostiquer, ne pas l'absorber.

**`REFERENCE_DIGEST` ne doit bouger dans aucune de ces sept tâches.** Rien ici ne change la simulation : on ajoute du code qui la lit. Si l'empreinte bouge, quelque chose d'inattendu écrit dans le monde — arrêter et chercher.

**Une autre session édite `front/src/app/game.ts`.** Les tâches 2 et 6 y touchent. Repérer les points d'accroche par symbole et non par numéro de ligne, et demander un worktree dédié avant de commencer.
