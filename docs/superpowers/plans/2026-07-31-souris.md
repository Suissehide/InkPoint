# Déplacement à la souris — plan d'implémentation

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Permettre de déplacer le point d'encre à la souris — le curseur devient une cible que le point poursuit — sans toucher au modèle de mouvement de la simulation.

**Architecture:** Deux sources d'entrée (`keyboard.ts`, `mouse.ts`) exposent la même interface `writeInto(input, player)` ; `game.ts` en appelle une seule par pas, selon un réglage persisté. Tout le comportement de la souris tient dans deux fonctions pures testables sans DOM ; seul le branchement des événements ne l'est pas. Un réticule dessiné par Pixi remplace le curseur système, masqué en jeu.

**Tech Stack:** TypeScript strict, PixiJS v8, bitECS, Vitest, Biome, Tailwind v4, Pointer Events.

**Spec :** `docs/superpowers/specs/2026-07-31-souris-design.md`

## Global Constraints

- Documentation et commentaires **en français**. Commits en Conventional Commits (commitlint est actif via Husky).
- `src/sim/` n'est pas modifié par ce lot. Sa pureté et son déterminisme sont vérifiés par `purity.test.ts` et `determinism.test.ts`.
- `src/render/` ne dépend jamais de `src/app/` : le réticule reçoit un type structurel `{ x: number; y: number } | null`, jamais un type importé de l'app.
- **Pas d'assertion non-nulle (`!`) hors de `src/sim/`** : `noNonNullAssertion` n'est désactivé que sur `src/sim/**` (biome.json). Utiliser `?? 0` ou une garde explicite.
- `noUncheckedIndexedAccess` est actif : tout accès indexé rend `T | undefined`.
- Toute valeur d'entrée écrite dans la simulation est **quantifiée à 1/128** (prérequis du netcode v3).
- Vérifications avant chaque commit : `npm run typecheck`, `npm run lint`, `npm test`.
- **Ne jamais pousser vers origin.**
- **Ne jamais `git add -A`** : un autre agent travaille parfois dans ce dépôt. Ajouter les fichiers un par un, exactement ceux listés par la tâche.
- Relire chaque fichier avant de le modifier.
- **Limite de vérification connue :** le ressenti (rayon de 32 px, zone morte de 3 px) ne se prouve pas au test unitaire. Il se juge en jouant (`npm run dev`). Ne jamais annoncer ces valeurs comme validées sur la seule foi des tests.
- Convention du dépôt suivie ici : les vues de rendu (`src/render/views/*`) et les sources d'entrée branchées sur le DOM n'ont pas de test unitaire — seules leurs fonctions pures extraites en ont (cf. `particles.test.ts`, `afterimage.test.ts`, et `keyboard.ts` qui n'a pas de test). Ne pas inventer de faux DOM pour contourner ça.

## Structure des fichiers

| Fichier | Responsabilité |
|---|---|
| `src/app/input-source.ts` *(nouveau)* | L'interface commune aux sources d'entrée, le type `Point`, et le réglage `MovementInput` avec sa résolution depuis le stockage |
| `src/app/mouse.ts` *(nouveau)* | Deux fonctions pures (conversion écran→arène, poursuite) + la coquille qui écoute `pointermove` |
| `src/app/mouse.test.ts` *(nouveau)* | Tests des deux fonctions pures |
| `src/app/input-source.test.ts` *(nouveau)* | Test du défaut du réglage |
| `src/app/keyboard.ts` | Se conforme à `InputSource` (son interface locale disparaît) |
| `src/render/views/reticle.ts` *(nouveau)* | Le réticule d'encre |
| `src/render/stage.ts` | Expose `setAimTarget` |
| `src/app/game.ts` | Choisit la source, distribue le viewport, pousse la cible au rendu |
| `src/ui/screens/settings.ts` | La ligne `Déplacement : Clavier / Souris` |
| `src/i18n/locales/{fr,en}.json` | Trois clés de réglage + l'indice du menu |

---

### Task 1 : les fonctions pures

**Files:**
- Create: `src/app/input-source.ts`
- Create: `src/app/mouse.ts`
- Test: `src/app/mouse.test.ts`
- Test: `src/app/input-source.test.ts`

**Interfaces:**
- Consumes: `Viewport` de `@/render/viewport` (champs `scale`, `x`, `y`, `arenaWidth`, `arenaHeight`), `InputState` de `@/sim/input` (champs `moveX`, `moveY`), `storage` de `./storage`.
- Produces:
  - `interface Point { x: number; y: number }`
  - `interface InputSource { writeInto(input: InputState, player: Point): void; destroy(): void }`
  - `type MovementInput = 'keyboard' | 'mouse'`
  - `function resolveMovementInput(): MovementInput`
  - `function screenToArena(clientX: number, clientY: number, viewport: Viewport): Point`
  - `function aimInput(player: Point, target: Point): { moveX: number; moveY: number }`

- [ ] **Step 1: Écrire les tests qui échouent**

Créer `src/app/mouse.test.ts` :

```ts
import { describe, expect, it } from 'vitest'

import type { Viewport } from '@/render/viewport'
import { aimInput, screenToArena } from './mouse'

/** Arène 1280×720 dans une fenêtre plus large : zoom 1, marge latérale de 100 px. */
const VIEWPORT: Viewport = { scale: 1, x: 100, y: 0, arenaWidth: 1280, arenaHeight: 720 }

describe('screenToArena', () => {
  it('retire le décalage du letterbox', () => {
    expect(screenToArena(100, 0, VIEWPORT)).toEqual({ x: 0, y: 0 })
  })

  it('divise par le zoom', () => {
    const zoomed: Viewport = { scale: 0.5, x: 0, y: 0, arenaWidth: 1280, arenaHeight: 720 }
    expect(screenToArena(320, 180, zoomed)).toEqual({ x: 640, y: 360 })
  })

  it("borne un point de la marge sur le bord de l'arène", () => {
    expect(screenToArena(0, 0, VIEWPORT)).toEqual({ x: 0, y: 0 })
    expect(screenToArena(9999, 9999, VIEWPORT)).toEqual({ x: 1280, y: 720 })
  })
})

describe('aimInput', () => {
  it('donne le plein régime au-delà du rayon', () => {
    const { moveX, moveY } = aimInput({ x: 0, y: 0 }, { x: 500, y: 0 })
    expect(moveX).toBe(1)
    expect(moveY).toBe(0)
  })

  it("décroît proportionnellement à l'intérieur du rayon", () => {
    // 16 px pour un rayon de 32 : moitié de régime.
    const { moveX } = aimInput({ x: 0, y: 0 }, { x: 16, y: 0 })
    expect(moveX).toBeCloseTo(0.5, 2)
  })

  it('rend une entrée nulle dans la zone morte', () => {
    expect(aimInput({ x: 100, y: 100 }, { x: 102, y: 100 })).toEqual({ moveX: 0, moveY: 0 })
  })

  it("ne calcule aucun angle quand la cible est confondue avec le joueur", () => {
    expect(aimInput({ x: 40, y: 40 }, { x: 40, y: 40 })).toEqual({ moveX: 0, moveY: 0 })
  })

  it('vise bien la cible en diagonale', () => {
    const { moveX, moveY } = aimInput({ x: 0, y: 0 }, { x: -300, y: -300 })
    expect(moveX).toBeCloseTo(-Math.SQRT1_2, 2)
    expect(moveY).toBeCloseTo(-Math.SQRT1_2, 2)
  })

  it('ne rend que des multiples de 1/128', () => {
    const { moveX, moveY } = aimInput({ x: 0, y: 0 }, { x: 137, y: -61 })
    expect(moveX * 128).toBeCloseTo(Math.round(moveX * 128), 10)
    expect(moveY * 128).toBeCloseTo(Math.round(moveY * 128), 10)
  })

  it('reste à magnitude 1 à un pas de quantification près', () => {
    // La quantification peut pousser chaque composante d'un demi-pas vers le
    // haut. `playerMovementSystem` renormalise toute entrée > 1 : ce test borne
    // le dépassement, il ne prétend pas qu'il n'existe pas.
    const { moveX, moveY } = aimInput({ x: 0, y: 0 }, { x: 400, y: 400 })
    expect(Math.hypot(moveX, moveY)).toBeLessThanOrEqual(1 + 2 / 128)
  })
})
```

Créer `src/app/input-source.test.ts` :

```ts
import { describe, expect, it } from 'vitest'

import { resolveMovementInput } from './input-source'

describe('resolveMovementInput', () => {
  // L'environnement Vitest est `node` : `localStorage` n'existe pas, `storage`
  // rattrape l'erreur et rend le défaut. C'est exactement le cas d'un premier
  // lancement, ou d'une navigation privée qui refuse le stockage.
  it('vaut « souris » sans rien de stocké', () => {
    expect(resolveMovementInput()).toBe('mouse')
  })
})
```

- [ ] **Step 2: Lancer les tests pour vérifier qu'ils échouent**

Run : `npx vitest run src/app/mouse.test.ts src/app/input-source.test.ts`
Expected : FAIL — `Failed to resolve import "./mouse"` et `"./input-source"`.

- [ ] **Step 3: Écrire `src/app/input-source.ts`**

```ts
import type { InputState } from '@/sim/input'
import { storage } from './storage'

/** Un point en coordonnées d'arène. */
export interface Point {
  x: number
  y: number
}

/**
 * Ce que toute source d'entrée sait faire. `game.ts` n'en appelle qu'une par
 * pas : la souris ayant toujours une position, composer les sources en
 * permanence tirerait le point en continu.
 *
 * `player` sert aux sources qui visent une cible ; le clavier l'ignore
 * simplement en ne le déclarant pas.
 */
export interface InputSource {
  writeInto(input: InputState, player: Point): void
  destroy(): void
}

export type MovementInput = 'keyboard' | 'mouse'

/**
 * Défaut : la souris. Toute valeur stockée qui n'est pas exactement
 * `'keyboard'` y retombe — un stockage corrompu ne doit pas rendre le jeu
 * injouable, seulement le ramener au défaut.
 */
export function resolveMovementInput(): MovementInput {
  return storage.get<string>('movementInput', 'mouse') === 'keyboard' ? 'keyboard' : 'mouse'
}
```

- [ ] **Step 4: Écrire les fonctions pures de `src/app/mouse.ts`**

```ts
import type { Viewport } from '@/render/viewport'
import type { Point } from './input-source'

/**
 * Au-delà de ce rayon, plein régime ; en deçà, l'intensité décroît et le point
 * se pose sur la cible au lieu de la dépasser en oscillant. Le point freine en
 * `v²/2a` = 240²/(2×2667) ≈ 11 px : 32 px laisse cette marge de freinage sans
 * amollir la course. Valeur de première passe, à régler en jouant.
 */
const FULL_THROTTLE_RADIUS = 32

/**
 * Sous ce seuil, l'entrée est nulle : la friction immobilise le point net, et
 * `Facing` conserve son dernier cap (`FACING_MIN_SPEED`) au lieu de frémir.
 * C'est aussi ce qui garantit qu'aucun angle n'est calculé sur une distance
 * nulle.
 */
const DEAD_ZONE = 3

/** Pas de quantification des entrées — prérequis du netcode v3 (spec §3.5). */
const QUANTUM = 1 / 128

function quantize(value: number): number {
  return Math.round(value / QUANTUM) * QUANTUM
}

/**
 * Position écran → coordonnées d'arène, bornée à l'arène. Le bornage n'est pas
 * cosmétique : l'arène est cadrée en letterbox (`computeViewport`), et sans lui
 * un curseur posé dans la marge tirerait le point vers un point hors du cadre
 * qu'il ne peut pas atteindre.
 */
export function screenToArena(clientX: number, clientY: number, viewport: Viewport): Point {
  const x = (clientX - viewport.x) / viewport.scale
  const y = (clientY - viewport.y) / viewport.scale
  return {
    x: Math.min(viewport.arenaWidth, Math.max(0, x)),
    y: Math.min(viewport.arenaHeight, Math.max(0, y)),
  }
}

/**
 * Poursuite : direction joueur→cible, intensité proportionnelle à la distance.
 * La sortie a la forme d'une entrée de manette — la simulation ne saura jamais
 * qu'une souris est derrière.
 */
export function aimInput(player: Point, target: Point): { moveX: number; moveY: number } {
  const dx = target.x - player.x
  const dy = target.y - player.y
  const distance = Math.hypot(dx, dy)
  if (distance <= DEAD_ZONE) {
    return { moveX: 0, moveY: 0 }
  }
  const intensity = Math.min(1, distance / FULL_THROTTLE_RADIUS)
  return {
    moveX: quantize((dx / distance) * intensity),
    moveY: quantize((dy / distance) * intensity),
  }
}
```

- [ ] **Step 5: Lancer les tests pour vérifier qu'ils passent**

Run : `npx vitest run src/app/mouse.test.ts src/app/input-source.test.ts`
Expected : PASS, 11 tests.

- [ ] **Step 6: Vérifications complètes**

Run : `npm run typecheck && npm run lint && npm test`
Expected : tout passe.

- [ ] **Step 7: Commit**

```bash
git add src/app/input-source.ts src/app/input-source.test.ts src/app/mouse.ts src/app/mouse.test.ts
git commit -m "feat(input): calculer la poursuite du curseur et la conversion écran-arène"
```

---

### Task 2 : la source souris et l'interface commune

**Files:**
- Modify: `src/app/mouse.ts` (ajout en fin de fichier)
- Modify: `src/app/keyboard.ts:8-17`

**Interfaces:**
- Consumes: `InputSource`, `Point` (Task 1), `screenToArena`, `aimInput` (Task 1), `Viewport` de `@/render/viewport`.
- Produces:
  - `interface MouseSource extends InputSource { setViewport(viewport: Viewport): void; target(): Point | null }`
  - `function createMouse(): MouseSource`
  - `function createKeyboard(): InputSource` — l'interface locale `Keyboard` disparaît (seul `game.ts` appelle `createKeyboard`, et il n'importe pas le type).

**Pas de test unitaire dans cette tâche** : elle ne contient que le branchement d'un écouteur `pointermove`, l'environnement Vitest est `node`, et le dépôt ne teste aucune coquille DOM (`keyboard.ts` n'a pas de test non plus). Toute la logique est déjà couverte par la Task 1. La vérification réelle a lieu à la Task 4, à la main.

- [ ] **Step 1: Ajouter `createMouse` à la fin de `src/app/mouse.ts`**

```ts
import type { InputState } from '@/sim/input'
import type { InputSource, Point } from './input-source'
```

(ajouter `InputState` et `InputSource` aux imports existants en tête de fichier — `Point` y est déjà)

```ts
export interface MouseSource extends InputSource {
  /** Rebranché par `game.ts` à chaque `applyLayout` : le zoom change avec la fenêtre. */
  setViewport(viewport: Viewport): void
  /**
   * La cible en coordonnées d'arène, ou `null` tant qu'aucun pointeur n'a
   * bougé — c'est ce `null` qui empêche le réticule d'apparaître au centre
   * d'une partie que personne ne pilote encore.
   */
  target(): Point | null
}

/**
 * Écoute `pointermove` en permanence, écrans compris : le joueur qui clique
 * « Jouer » a déjà donné une position, et la partie démarre donc avec une cible
 * plutôt qu'à vide. Rien n'est réinitialisé quand le curseur quitte la fenêtre —
 * la dernière cible tient, le point la rejoint et s'y arrête.
 */
export function createMouse(): MouseSource {
  let viewport: Viewport | null = null
  let clientX = 0
  let clientY = 0
  let moved = false

  const onMove = (event: PointerEvent): void => {
    clientX = event.clientX
    clientY = event.clientY
    moved = true
  }

  window.addEventListener('pointermove', onMove)

  const target = (): Point | null => {
    if (!moved || viewport === null) {
      return null
    }
    return screenToArena(clientX, clientY, viewport)
  }

  return {
    setViewport(next: Viewport): void {
      viewport = next
    },

    target,

    writeInto(input: InputState, player: Point): void {
      const aim = target()
      if (aim === null) {
        input.moveX = 0
        input.moveY = 0
        return
      }
      const { moveX, moveY } = aimInput(player, aim)
      input.moveX = moveX
      input.moveY = moveY
    },

    destroy(): void {
      window.removeEventListener('pointermove', onMove)
    },
  }
}
```

- [ ] **Step 2: Conformer `src/app/keyboard.ts`**

Remplacer les lignes 1 et 8-17 (l'interface locale `Keyboard` et la signature de `createKeyboard`) :

```ts
import type { InputState } from '@/sim/input'
import type { InputSource } from './input-source'
```

et supprimer le bloc :

```ts
export interface Keyboard {
  writeInto(input: InputState): void
  destroy(): void
}
```

puis :

```ts
/**
 * ZQSD, WASD et les flèches sont actifs simultanément : on lit les codes
 * physiques (`event.code`), donc la disposition du clavier n'a pas d'importance.
 *
 * Se conforme à `InputSource` sans déclarer son second paramètre : le clavier
 * n'a aucun besoin de savoir où est le joueur.
 */
export function createKeyboard(): InputSource {
```

Le corps de la fonction ne change pas d'une ligne.

- [ ] **Step 3: Vérifications**

Run : `npm run typecheck && npm run lint && npm test`
Expected : tout passe. `typecheck` est la vérification qui compte ici : c'est lui qui prouve que `createKeyboard` satisfait bien `InputSource` malgré son `writeInto` à un seul paramètre.

- [ ] **Step 4: Commit**

```bash
git add src/app/mouse.ts src/app/keyboard.ts
git commit -m "feat(input): brancher la souris derrière l'interface commune des sources"
```

---

### Task 3 : le réticule

**Files:**
- Create: `src/render/views/reticle.ts`
- Modify: `src/render/stage.ts` (interface `Stage`, construction, objet rendu)

**Interfaces:**
- Consumes: `INK` de `../ink`, `Container`/`Graphics` de `pixi.js`.
- Produces:
  - `interface ReticleView { container: Container; update(target: { x: number; y: number } | null): void }`
  - `function createReticleView(): ReticleView`
  - `Stage.setAimTarget(target: { x: number; y: number } | null): void`

Le type de la cible est **structurel**, jamais importé de `src/app/` : le rendu ne dépend pas de l'app.

Pas de test unitaire : c'est une vue Pixi, et le dépôt n'en teste aucune (cf. `views/player.ts`, `views/enemy.ts`). Vérification visuelle à la Task 4.

- [ ] **Step 1: Créer `src/render/views/reticle.ts`**

```ts
import { Container, Graphics } from 'pixi.js'

import { INK } from '../ink'

export interface ReticleView {
  container: Container
  update(target: { x: number; y: number } | null): void
}

/** Longueur d'un bras, et vide laissé au centre pour ne pas masquer la cible. */
const ARM = 7
const GAP = 4

/**
 * La cible du déplacement à la souris : quatre traits d'encre autour d'un
 * point. Il remplace le curseur système, masqué pendant la partie (spec §3.2) —
 * le jeu étant lui-même « un curseur », deux curseurs à l'écran prêteraient à
 * confusion.
 */
export function createReticleView(): ReticleView {
  const container = new Container()
  const gfx = new Graphics()
  container.addChild(gfx)
  // Masqué tant que personne n'a poussé de cible : sans ça, un réticule
  // resterait posé à l'origine de l'arène avant le premier `update`.
  container.visible = false

  gfx
    .moveTo(-GAP - ARM, 0)
    .lineTo(-GAP, 0)
    .moveTo(GAP, 0)
    .lineTo(GAP + ARM, 0)
    .moveTo(0, -GAP - ARM)
    .lineTo(0, -GAP)
    .moveTo(0, GAP)
    .lineTo(0, GAP + ARM)
    .stroke({ color: INK.paper, width: 2, alpha: 0.7 })
  gfx.circle(0, 0, 1.5).fill({ color: INK.paper, alpha: 0.7 })

  return {
    container,
    update(target): void {
      container.visible = target !== null
      if (target !== null) {
        container.x = target.x
        container.y = target.y
      }
    },
  }
}
```

- [ ] **Step 2: Déclarer `setAimTarget` dans l'interface `Stage`**

Dans `src/render/stage.ts`, ajouter juste après `setDangerProximity(v: number): void` (ligne 86) :

```ts
  /**
   * Cible du déplacement à la souris, en coordonnées d'arène — `null` la
   * masque. Poussée par `app/game.ts` à chaque image : elle vit en temps réel,
   * pas en temps de simulation, et n'est donc jamais interpolée.
   */
  setAimTarget(target: { x: number; y: number } | null): void
```

- [ ] **Step 3: Construire la vue et l'exposer**

Ajouter l'import (groupe `:PATH:`, après `createPlayerView`) :

```ts
import { createReticleView } from './views/reticle'
```

Juste après `worldLayer.addChild(playerView.container)` :

```ts
  // Dans `worldLayer` comme le joueur : le réticule est un trait d'encre, il
  // doit frémir sous le boil comme le reste de l'arène. Ajouté après le joueur,
  // donc dessiné par-dessus lui.
  const reticle = createReticleView()
  worldLayer.addChild(reticle.container)
```

Dans l'objet rendu, juste après `setDangerProximity,` :

```ts
    setAimTarget(target: { x: number; y: number } | null): void {
      reticle.update(target)
    },
```

- [ ] **Step 4: Vérifications**

Run : `npm run typecheck && npm run lint && npm test`
Expected : tout passe. Le réticule n'a encore aucun appelant, et son conteneur naît masqué (Step 1) : le rendu est strictement inchangé à l'écran. C'est la Task 4 qui l'allume.

- [ ] **Step 5: Commit**

```bash
git add src/render/views/reticle.ts src/render/stage.ts
git commit -m "feat(render): dessiner le réticule de la cible souris"
```

---

### Task 4 : brancher la souris dans le jeu

**Files:**
- Modify: `src/app/game.ts`

**Interfaces:**
- Consumes: `createMouse` (Task 2), `resolveMovementInput`, `MovementInput`, `Point` (Task 1), `stage.setAimTarget` (Task 3), `Position` de `@/sim/components`.
- Produces: rien de nouveau pour les tâches suivantes, hormis la variable de closure `movementInput` que la Task 5 viendra piloter.

> **Déjà fait à la Task 2.** L'import de `Position`, l'import de `Point` et la
> fonction `playerPoint()` ont été remontés dans la Task 2 : dès que
> `createKeyboard()` rend `InputSource`, l'appel `keyboard.writeInto(input)` à un
> seul argument ne compile plus, et la Task 2 ne pouvait pas laisser l'arbre vert
> sans corriger ce site d'appel. Les Steps 1 et 3 ci-dessous en tiennent compte.

- [ ] **Step 1: Compléter les imports**

Dans `src/app/game.ts`, l'import de `./input-source` existe déjà (`import type { Point } from './input-source'`). L'étendre :

```ts
import { type MovementInput, type Point, resolveMovementInput } from './input-source'
```

Et ajouter, dans le même groupe `:PATH:` :

```ts
import { createMouse } from './mouse'
```

(Biome trie ce groupe : `./game-state`, `./input-source`, `./juice`, `./keyboard`, `./loop`, `./mouse`, `./storage`. Laisser `npm run format` remettre l'ordre si besoin.)

- [ ] **Step 2: Créer la source et lire le réglage**

Juste après `const keyboard = createKeyboard()` (ligne 70) :

```ts
  const mouse = createMouse()
```

Puis, à côté de `let reducedMotion = resolveReducedMotion()` :

```ts
  // Défaut souris (spec) ; la Task 5 branche l'écran Réglages dessus.
  let movementInput: MovementInput = resolveMovementInput()
```

- [ ] **Step 3: *(fait à la Task 2)* `playerPoint()`**

La fonction existe déjà dans `src/app/game.ts`, juste après `startRun()`. Rien à
écrire ; vérifier seulement qu'elle est bien là avant le Step 4, qui l'appelle.

- [ ] **Step 4: Choisir la source dans `onStep`**

Remplacer, dans `createFixedLoop({ onStep… })` :

```ts
        keyboard.writeInto(run.world.input, playerPoint())
```

par :

```ts
        // Une seule source par pas, jamais les deux : la souris ayant toujours
        // une position, les composer la ferait tirer le point en continu.
        const source = movementInput === 'mouse' ? mouse : keyboard
        source.writeInto(run.world.input, playerPoint())
```

- [ ] **Step 5: Pousser la cible au rendu**

Dans `onRender`, juste avant `stage.sync(run.world, alpha)` :

```ts
      // Le réticule apparaît exactement quand le curseur système disparaît :
      // `cursorHidden` vient d'être recalculé par `syncCursorVisibility()`
      // ci-dessus. Pause, choix de carte et game over le font donc disparaître
      // avec le curseur qu'il remplace.
      stage.setAimTarget(movementInput === 'mouse' && cursorHidden ? mouse.target() : null)
```

- [ ] **Step 6: Distribuer le viewport**

Dans `applyLayout`, après `hud.setViewport(viewport)` :

```ts
    // Sans cette ligne, la conversion écran→arène resterait calée sur le zoom
    // d'avant le redimensionnement.
    mouse.setViewport(viewport)
```

- [ ] **Step 7: Vérifications automatiques**

Run : `npm run typecheck && npm run lint && npm test`
Expected : tout passe.

- [ ] **Step 8: Vérification manuelle — c'est ici que le lot se juge**

Run : `npm run dev`, ouvrir l'URL affichée, lancer une partie.

Attendu :
1. Le réticule d'encre suit le curseur, le curseur système reste masqué.
2. Le point d'encre poursuit le réticule et s'arrête dessus sans osciller.
3. `Échap` met en pause : le réticule disparaît, le curseur système revient.
4. Reprendre : le réticule revient.
5. Redimensionner la fenêtre en pleine partie : le réticule reste sous le curseur physique.
6. Sortir le curseur de la fenêtre : le point rejoint la dernière cible et s'arrête.
7. Fin de vague (écran de carte) : réticule masqué, souris utilisable dans les cartes.

**Noter ce qui se ressent mal** — notamment si 32 px rend l'arrêt mou ou nerveux. Ne rien régler à l'aveugle : remonter l'observation, le réglage de la valeur est une décision du propriétaire du projet.

- [ ] **Step 9: Commit**

```bash
git add src/app/game.ts
git commit -m "feat(app): déplacer le point d'encre à la souris"
```

---

### Task 5 : le réglage dans l'écran Réglages

**Files:**
- Modify: `src/ui/screens/settings.ts`
- Modify: `src/i18n/locales/fr.json`
- Modify: `src/i18n/locales/en.json`
- Modify: `src/app/game.ts` (le callback)

**Interfaces:**
- Consumes: `MovementInput`, `resolveMovementInput` (Task 1), `movementInput` dans `game.ts` (Task 4).
- Produces: `SettingsDeps.onMovementInputChange(next: MovementInput): void`.

Les lignes de l'écran sont **renumérotées** : `Langue` 0, `Déplacement` 1, `Mouvement réduit` 2, `Effets sonores` 3, `Retour` 4. C'est le point le plus facile à rater de cette tâche — `activate` et `handleKey` testent les index en dur.

- [ ] **Step 1: Ajouter les clés i18n**

Dans `src/i18n/locales/fr.json`, après `"settings.reducedMotion"` :

```json
  "settings.movement": "Déplacement",
  "settings.movementKeyboard": "Clavier",
  "settings.movementMouse": "Souris",
```

Dans `src/i18n/locales/en.json`, au même endroit :

```json
  "settings.movement": "Movement",
  "settings.movementKeyboard": "Keyboard",
  "settings.movementMouse": "Mouse",
```

- [ ] **Step 2: Lancer la parité i18n**

Run : `npx vitest run src/i18n/parity.test.ts`
Expected : PASS — les deux dictionnaires ont exactement les mêmes clés.

- [ ] **Step 3: Modifier `src/ui/screens/settings.ts`**

Ajouter à l'import de `@/app/storage` la ligne voisine :

```ts
import { type MovementInput, resolveMovementInput } from '@/app/input-source'
```

Étendre `SettingsDeps` :

```ts
export interface SettingsDeps {
  /** Branché sur `stage.setEffects` par `game.ts` (spec §6.8). */
  onReducedMotionChange(reduced: boolean): void
  /** Branché sur le choix de source d'entrée de `game.ts`. */
  onMovementInputChange(next: MovementInput): void
}
```

Passer le compte de lignes à 5 :

```ts
// Langue, déplacement, mouvement réduit, volume des effets, retour.
const ROW_COUNT = 5
```

Déclarer l'état, à côté de `let reducedMotion` :

```ts
  let movementInput = resolveMovementInput()
```

Ajouter le libellé, à côté de `languageLabel` :

```ts
  const movementLabel = (input: MovementInput): string =>
    input === 'mouse' ? t('settings.movementMouse') : t('settings.movementKeyboard')
```

Ajouter la bascule, à côté de `toggleReducedMotion` :

```ts
  const toggleMovementInput = (): void => {
    movementInput = movementInput === 'mouse' ? 'keyboard' : 'mouse'
    storage.set('movementInput', movementInput)
    deps.onMovementInputChange(movementInput)
    render()
  }
```

Remplacer `activate` **en entier** :

```ts
  const activate = (index: number): void => {
    if (index === 0) {
      toggleLanguage()
    } else if (index === 1) {
      toggleMovementInput()
    } else if (index === 2) {
      toggleReducedMotion()
    } else if (index === 4) {
      back()
    }
    // La ligne 3 (volume) n'a pas d'activation générique : voir `volumeControls`
    // et les écouteurs posés directement sur ses boutons ci-dessous.
  }
```

Remplacer le bloc de lignes de `render` **en entier** :

```ts
        ${row(0, t('settings.language'), languageLabel(getLocale()))}
        ${row(1, t('settings.movement'), movementLabel(movementInput))}
        ${row(2, t('settings.reducedMotion'), reducedMotion ? t('settings.on') : t('settings.off'))}
        ${row(3, t('settings.sfxVolume'), `${sfxVolume}%`, volumeControls)}
        ${row(4, t('settings.back'), '')}
```

Dans `show()`, relire la valeur persistée à côté des deux autres :

```ts
      movementInput = resolveMovementInput()
```

Remplacer le bloc gauche/droite de `handleKey` **en entier** :

```ts
      if (NAV_LEFT_CODES.includes(code) || NAV_RIGHT_CODES.includes(code)) {
        const dir = NAV_LEFT_CODES.includes(code) ? -1 : 1
        if (nav.index === 0) {
          toggleLanguage()
        } else if (nav.index === 1) {
          toggleMovementInput()
        } else if (nav.index === 3) {
          adjustVolume(dir * VOLUME_STEP)
        }
        return true
      }
```

- [ ] **Step 4: Fournir le callback dans `src/app/game.ts`**

Dans `createSettingsScreen(uiRoot, { … })`, après `onReducedMotionChange` :

```ts
    onMovementInputChange(next): void {
      movementInput = next
    },
```

- [ ] **Step 5: Vérifications automatiques**

Run : `npm run typecheck && npm run lint && npm test`
Expected : tout passe.

- [ ] **Step 6: Vérification manuelle**

Run : `npm run dev`

1. Menu → Réglages : la ligne `Déplacement` affiche `Souris`.
2. `↑ ↓` navigue sur cinq lignes, `←`/`→` **et** le clic basculent `Déplacement`.
3. Basculer sur `Clavier`, jouer : ZQSD/flèches déplacent le point, aucun réticule, la souris ne fait rien en jeu.
4. Rebasculer sur `Souris` depuis la pause en pleine partie : le réticule réapparaît à la reprise, le point reprend sa poursuite (la vélocité en cours n'est pas coupée).
5. Recharger la page : le choix est conservé.
6. Vérifier que `Effets sonores` (+/−) et `Retour` répondent toujours — ce sont les deux lignes renumérotées.

- [ ] **Step 7: Commit**

```bash
git add src/ui/screens/settings.ts src/app/game.ts src/i18n/locales/fr.json src/i18n/locales/en.json
git commit -m "feat(ui): choisir clavier ou souris dans les réglages"
```

---

### Task 6 : la documentation

**Files:**
- Modify: `README.md`
- Modify: `docs/superpowers/specs/2026-07-28-inkpoint-rebuild-design.md` (§3.2 et §6.8)
- Modify: `src/i18n/locales/fr.json`, `src/i18n/locales/en.json` (`menu.hint`)

Trois affirmations du dépôt sont devenues fausses ; cette tâche les corrige. Ne rien supprimer sèchement : c'est la nuance qu'il faut écrire.

- [ ] **Step 1: `menu.hint`**

L'indice du menu annonce les commandes ; il ne cite plus la commande par défaut.

`fr.json` : `"menu.hint": "Souris ou ZQSD / Flèches · Échap",`
`en.json` : `"menu.hint": "Mouse or WASD / Arrows · Esc",`

- [ ] **Step 2: `README.md`**

Remplacer le paragraphe d'introduction :

```markdown
Dodge the ink. A roguelike survival game where you play a cursor, enemies
materialise out of the page, and your only weapons are the power-ups you pick up
along the way.
```

Remplacer la table des commandes et la phrase qui la suit :

```markdown
## Controls

| Action          | Input                                          |
| --------------- | ---------------------------------------------- |
| Move (mouse)    | The ink point chases your cursor — the default |
| Move (keyboard) | `WASD` / `ZQSD` / Arrow keys                   |
| Power-ups       | None — they fire the instant you touch them    |
| Pause           | `Esc`                                          |

Pick your movement device in Settings. Still fully playable with the keyboard
alone. Available in English and French.
```

- [ ] **Step 3: Spec de fond §3.2**

Remplacer la ligne `Déplacement` de la table :

```markdown
| Déplacement | Souris (défaut) **ou** `ZQSD` **et** `WASD` **et** flèches (les trois actifs simultanément) |
```

Remplacer le paragraphe « Pendant la partie, en revanche… » :

```markdown
Pendant la partie, la souris déplace le point : le curseur devient une cible que
le point poursuit, et le curseur système reste masqué sur l'arène — c'est un
réticule d'encre qui le remplace (voir `2026-07-31-souris-design.md`). Il
réapparaît dès qu'un écran s'ouvre. Le mode se choisit dans les Réglages, et le
jeu reste intégralement jouable au clavier seul.
```

- [ ] **Step 4: Spec de fond §6.8**

Remplacer la puce :

```markdown
- Tout au clavier, aucune dépendance à la souris — la souris est le mode de
  déplacement par défaut, mais le mode Clavier s'atteint au clavier seul, dans
  les Réglages.
```

- [ ] **Step 5: Vérifications**

Run : `npm run typecheck && npm run lint && npm test`
Expected : tout passe (`parity.test.ts` couvre `menu.hint` des deux côtés).

Relire les deux fichiers Markdown modifiés : aucune autre phrase du dépôt ne doit encore prétendre que la souris ne sert à rien en jeu.

Run : `grep -rn "keyboard-only\|clavier seul\|ne sert à rien" README.md docs/superpowers/specs/2026-07-28-inkpoint-rebuild-design.md`
Expected : ne restent que les formulations corrigées ci-dessus.

- [ ] **Step 6: Commit**

```bash
git add README.md docs/superpowers/specs/2026-07-28-inkpoint-rebuild-design.md src/i18n/locales/fr.json src/i18n/locales/en.json
git commit -m "docs: annoncer la souris comme mode de déplacement par défaut"
```
